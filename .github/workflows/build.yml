  name: Build

  on:
    workflow_dispatch:
    pull_request_review:
      types:
        - submitted

        
  env:
    UNITY_EMAIL: ${{ secrets.UNITY_EMAIL }}
    UNITY_PASSWORD: ${{ secrets.UNITY_PASSWORD }}
    UNITY_SERIAL: ${{ secrets.UNITY_SERIAL }}

  jobs:   
    build:
      name: Build
      runs-on: ubuntu-latest
      concurrency:
        group: build-a-${{ github.ref }}
        cancel-in-progress: true
      steps:
          - name: Checkout code
            uses: actions/checkout@v3
            with:
              lfs: true              
          - uses: actions/cache@v3
            with:
              path: Library
              key: Library-build
              restore-keys: Library-build

          - uses: game-ci/unity-builder@v2
            with:
              unityVersion: 2022.3.4f1
              buildMethod: Inworld.Editors.UnityPackageExporter.ExportFull
          - name: upload-artifact
            uses: actions/upload-artifact@v3
            with:
              name: InworldAI.Full
              path: Build/InworldAI.Full.unitypackage

          - uses: game-ci/unity-builder@v2
            with:
              unityVersion: 2022.3.4f1
              buildMethod: Inworld.Editors.UnityPackageExporter.ExportLite
          - name: upload-artifact
            uses: actions/upload-artifact@v3
            with:
              name: InworldAI.Lite
              path: Build/InworldAI.Lite.unitypackage 
